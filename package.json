{
  "name": "imicros-key-value-store",
  "version": "0.0.1",
  "author": "Andreas Leinen <andreas.leinen@imicros.de> (www.imicros.de)",
  "license": "MIT",
  "description": "Moleculer service for key value store based on Redis database (with authentification and authorization by imicros-auth) ",
  "main": "index.js",
  "scripts": {
    "lint": "eslint . --ext .js,.jsx",
    "test-store": "jest test/store.spec.js --no-cache",
    "test": "jest --coverage --no-cache"
  },
  "jest": {
    "collectCoverageFrom": [
      "lib/*.js",
      "lib/util/*.js",
      "!lib/globals.js"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/al66/imicros-key-value-store.git"
  },
  "keywords": [
    "imicros",
    "moleculer",
    "key-value",
    "store"
  ],
  "devDependencies": {
    "coveralls": "^3.0.1",
    "eslint": "^4.19.1",
    "ioredis-mock": "^3.10.1",
    "jest": "^22.4.3",
    "moleculer": "^0.12.3",
    "nats": "^0.8.10"
  },
  "dependencies": {
    "imicros-auth": "^0.0.4",
    "ioredis": "^3.2.2"
  }
}
